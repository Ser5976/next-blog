# План разработки Dashboard Comments

## 📋 Обзор

Создание страницы управления комментариями в dashboard по аналогии с `dashboard-users`, следуя архитектуре FSD.

---

## 🎯 Функциональные требования

### Основной функционал:
1. ✅ **Получение всех комментариев** с пагинацией
2. ✅ **Поиск комментариев** по:
   - Содержимому комментария
   - Автору (email/имя)
   - Посту (название)
3. ✅ **Редактирование комментария** (изменение содержимого)
4. ✅ **Удаление комментария** с подтверждением
5. ✅ **Отображение информации**:
   - Когда написан (createdAt)
   - Кем написан (автор: имя, email, аватар)
   - К какому посту относится (название, slug, ссылка)
   - Статистика (лайки, дизлайки)
6. ✅ **Пагинация** (верхняя и нижняя)
7. ✅ **Refresh** данных
8. ✅ **Префетчинг** следующей страницы

---

## 🏗️ Архитектура (FSD)

### Структура виджета `dashboard-comments`:

```
src/widgets/dashboard-comments/
├── api/
│   ├── comments-api.ts          # API функции (getComments, updateComment, deleteComment)
│   ├── index.ts
│
├── hooks/
│   ├── use-comments-management.ts  # Главный хук управления состоянием
│   ├── use-comments.ts             # Хук загрузки комментариев (React Query)
│   ├── use-update-comment.ts       # Хук обновления комментария (mutation)
│   ├── use-delete-comment.ts       # Хук удаления комментария (mutation)
│   ├── use-prefetch-comments.ts    # Хук префетчинга следующей страницы
│   ├── index.ts
│
├── lib/
│   ├── constants.ts            # Константы (заголовки, подзаголовки)
│   ├── index.ts
│
├── model/
│   ├── types.ts                # Типы и интерфейсы
│   ├── index.ts
│
├── ui/
│   ├── dashboard-comments.tsx      # Главный компонент
│   ├── comment-row.tsx             # Компонент строки комментария
│   ├── comments-filters.tsx        # Компонент фильтров поиска
│   ├── index.ts
│
└── index.ts
```

---

## 📄 Детальный план реализации

### Этап 1: Модели и типы (`model/`)

#### 1.1. `model/types.ts`

**Интерфейсы:**

```typescript
// Фильтры для поиска комментариев
export interface CommentsFilters {
  page: number;
  limit: number;
  search?: string;              // Поиск по содержимому, автору, посту
}

// Ответ API для списка комментариев
export interface CommentsResponse {
  success: boolean;
  comments: Comment[];
  total: number;
  page: number;
  totalPages: number;
  message?: string;
}

// Расширенный тип комментария для dashboard
export interface DashboardComment {
  id: string;
  content: string;
  createdAt: number | null;      // timestamp
  updatedAt: number | null;     // timestamp
  author: {
    id: string;
    email: string;
    firstName: string | null;
    lastName: string | null;
    imageUrl: string;
    role: string;
  } | null;
  post: {
    id: string;
    title: string;
    slug: string;
    published: boolean;
  };
  stats: {
    likesCount: number;
    dislikesCount: number;
  };
}

// Параметры обновления комментария
export interface UpdateCommentParams {
  commentId: string;
  content: string;
}

// Параметры удаления комментария
export interface DeleteCommentParams {
  commentId: string;
}

// Общий ответ API
export interface ApiResponse {
  success: boolean;
  message: string;
}

// Props для CommentRow
export interface CommentRowProps {
  comment: DashboardComment;
  onEdit: (commentId: string, currentContent: string) => void;
  onDelete: (commentId: string, content: string) => void;
  isEditing?: boolean;
  isDeleting?: boolean;
}

// Props для CommentsFilters
export interface CommentsFiltersProps {
  filters: CommentsFilters;
  onFiltersChange: (filters: CommentsFilters) => void;
}
```

---

### Этап 2: Константы (`lib/`)

#### 2.1. `lib/constants.ts`

```typescript
export const DASHBOARD_COMMENTS_CONFIG = {
  title: 'Comment Management',
  subtitle: 'Manage and moderate all comments',
} as const;
```

---

### Этап 3: API функции (`api/`)

#### 3.1. `api/comments-api.ts`

**Функции:**

1. **`getComments(filters: CommentsFilters): Promise<CommentsResponse>`**
   - GET запрос к `/api/dashboard/comments`
   - Параметры: `page`, `limit`, `search`
   - Возвращает список комментариев с пагинацией

2. **`updateComment(params: UpdateCommentParams): Promise<ApiResponse>`**
   - PATCH запрос к `/api/dashboard/comments/[commentId]`
   - Body: `{ content: string }`
   - Обновляет содержимое комментария

3. **`deleteComment(commentId: string): Promise<ApiResponse>`**
   - DELETE запрос к `/api/dashboard/comments/[commentId]`
   - Удаляет комментарий (endpoint уже существует)

---

### Этап 4: API Route Handlers (Backend)

#### 4.1. `src/app/api/dashboard/comments/route.ts` (создать/обновить)

**GET handler:**
- Получение списка комментариев с фильтрацией и пагинацией
- Параметры запроса:
  - `page` (number)
  - `limit` (number)
  - `search` (string, optional) - поиск по содержимому, автору, названию поста
- Возвращает:
  - Список комментариев с информацией об авторе и посте
  - Статистику (лайки, дизлайки)
  - Метаданные пагинации

**Логика поиска:**
- По содержимому комментария (`content`)
- По email/имени автора (через Clerk)
- По названию поста (`post.title`)

#### 4.2. `src/app/api/dashboard/comments/[commentId]/route.ts` (обновить)

**PATCH handler:**
- Обновление содержимого комментария
- Проверка прав (admin/author)
- Валидация данных
- Возвращает обновленный комментарий

**DELETE handler:** (уже существует, проверить совместимость)

---

### Этап 5: Hooks (`hooks/`)

#### 5.1. `hooks/use-comments.ts`

**React Query hook для загрузки комментариев:**
- Query key: `['comments', 'list', filters]`
- Query function: `getComments(filters)`
- Опции:
  - `staleTime: 30000` (30 секунд)
  - `gcTime: 300000` (5 минут)
  - `retry: 2`

#### 5.2. `hooks/use-update-comment.ts`

**React Query mutation для обновления комментария:**
- Mutation function: `updateComment(params)`
- Оптимистичное обновление:
  - Обновляет комментарий в кэше списка
  - Откатывает при ошибке
- Инвалидация:
  - `['comments', 'list']` - все списки
  - `['comments', commentId]` - конкретный комментарий
- Toast уведомления (success/error)

#### 5.3. `hooks/use-delete-comment.ts`

**React Query mutation для удаления комментария:**
- Mutation function: `deleteComment(commentId)`
- Оптимистичное удаление:
  - Удаляет комментарий из кэша
  - Откатывает при ошибке
- Инвалидация:
  - `['comments', 'list']` - все списки
- Toast уведомления (success/error)

#### 5.4. `hooks/use-prefetch-comments.ts`

**Хук для префетчинга следующей страницы:**
- Использует `queryClient.prefetchQuery`
- Вызывается при hover на список или при переходе на следующую страницу

#### 5.5. `hooks/use-comments-management.ts`

**Главный хук управления состоянием:**
- Объединяет все hooks
- Управляет:
  - Фильтрами (state)
  - Диалогом удаления (state)
  - Диалогом редактирования (state)
  - Дебаунсом поиска
- Обработчики:
  - `handlePageChange`
  - `handleItemsPerPageChange`
  - `handleFiltersChange`
  - `handleEditClick`
  - `handleConfirmEdit`
  - `handleCancelEdit`
  - `handleDeleteClick`
  - `handleConfirmDelete`
  - `handleCancelDelete`
  - `handleRefresh`
  - `handlePrefetchNextPage`
- Helpers:
  - `isCommentEditing(commentId)`
  - `isCommentDeleting(commentId)`

**Переиспользование:**
- `useCustomDebounce` из `dashboard-users` (или создать общий в shared)

---

### Этап 6: UI компоненты (`ui/`)

#### 6.1. `ui/comments-filters.tsx`

**Компонент фильтров поиска:**
- Input для поиска
- Иконка поиска
- Tooltip с подсказками
- Дебаунс (через хук)
- Сброс page на 1 при изменении поиска

**Props:**
- `filters: CommentsFilters`
- `onFiltersChange: (filters: CommentsFilters) => void`

**Accessibility:**
- `role="search"`
- `aria-label` на input

#### 6.2. `ui/comment-row.tsx`

**Компонент строки комментария:**
- Отображение информации:
  - Аватар автора (или placeholder)
  - Имя и email автора
  - Содержимое комментария
  - Название поста (ссылка)
  - Дата создания
  - Статистика (лайки/дизлайки)
- Действия:
  - Кнопка "Edit" (открывает диалог редактирования)
  - Кнопка "Delete" (открывает диалог подтверждения)
  - Dropdown меню с действиями
- Состояния:
  - Loading при редактировании/удалении
  - Disabled во время операций

**Props:**
- `comment: DashboardComment`
- `onEdit: (commentId, currentContent) => void`
- `onDelete: (commentId, content) => void`
- `isEditing?: boolean`
- `isDeleting?: boolean`

**Accessibility:**
- `role="row"`
- `aria-label` с информацией о комментарии
- `aria-busy` при операциях

#### 6.3. `ui/dashboard-comments.tsx`

**Главный компонент:**
- Заголовок и подзаголовок
- Кнопка Refresh
- Карточка с комментариями:
  - Заголовок с иконкой
  - Описание (количество комментариев)
  - Верхняя пагинация
  - Фильтры
  - Список комментариев:
    - Loading skeleton
    - Empty state
    - Список CommentRow
  - Нижняя пагинация
- Диалог редактирования:
  - Textarea для редактирования содержимого
  - Кнопки Confirm/Cancel
  - Loading состояние
- Диалог удаления:
  - Подтверждение удаления
  - Показ содержимого комментария
  - Кнопки Confirm/Cancel
  - Loading состояние

**Использует:**
- `useCommentsManagement` hook
- `UniversalError` для ошибок
- `UniversalEmpty` для пустого состояния
- `ListSkeleton` для загрузки
- `Pagination` компонент
- `ConfirmDialog` для удаления
- Кастомный диалог для редактирования

---

### Этап 7: Страница (`app/`)

#### 7.1. `src/app/dashboard/comments/page.tsx`

```typescript
import { DashboardComments } from '@/widgets/dashboard-comments';

export default async function DashboardCommentsPage() {
  return <DashboardComments />;
}
```

---

### Этап 8: Навигация

#### 8.1. Обновить `src/widgets/dashboard-sidebar/lib/nav-items.ts`

Добавить пункт меню:
```typescript
{
  title: 'Comments',
  href: '/dashboard/comments',
  icon: MessageSquare,
}
```

---

## 🔄 Последовательность реализации

### Фаза 1: Основа (Backend + Models)
1. ✅ Создать структуру папок виджета
2. ✅ Определить типы в `model/types.ts`
3. ✅ Создать константы в `lib/constants.ts`
4. ✅ Создать API route: `GET /api/dashboard/comments`
5. ✅ Создать API route: `PATCH /api/dashboard/comments/[commentId]`
6. ✅ Проверить `DELETE /api/dashboard/comments/[commentId]`

### Фаза 2: API и Hooks
7. ✅ Создать `api/comments-api.ts`
8. ✅ Создать `hooks/use-comments.ts`
9. ✅ Создать `hooks/use-update-comment.ts`
10. ✅ Создать `hooks/use-delete-comment.ts`
11. ✅ Создать `hooks/use-prefetch-comments.ts`
12. ✅ Создать `hooks/use-comments-management.ts`

### Фаза 3: UI компоненты
13. ✅ Создать `ui/comments-filters.tsx`
14. ✅ Создать `ui/comment-row.tsx`
15. ✅ Создать `ui/dashboard-comments.tsx`

### Фаза 4: Интеграция
16. ✅ Создать страницу `app/dashboard/comments/page.tsx`
17. ✅ Добавить пункт в навигацию
18. ✅ Экспортировать виджет в `index.ts`

### Фаза 5: Тестирование и доработка
19. ✅ Проверить все функции
20. ✅ Проверить обработку ошибок
21. ✅ Проверить accessibility
22. ✅ Оптимизировать производительность

---

## 📝 Дополнительные детали

### Поиск комментариев

**Логика поиска в API:**
```typescript
// Псевдокод
if (search) {
  where: {
    OR: [
      { content: { contains: search, mode: 'insensitive' } },
      { author: { email: { contains: search } } },
      { post: { title: { contains: search, mode: 'insensitive' } } }
    ]
  }
}
```

### Редактирование комментария

**Диалог редактирования:**
- Textarea с текущим содержимым
- Валидация (минимум символов, максимум символов)
- Кнопки: Save, Cancel
- Loading состояние при сохранении
- Toast уведомления

### Удаление комментария

**Диалог удаления:**
- Показ содержимого комментария (первые N символов)
- Предупреждение о необратимости
- Кнопки: Delete, Cancel
- Loading состояние при удалении
- Toast уведомления

### Отображение информации

**CommentRow должен показывать:**
1. **Автор:**
   - Аватар (или placeholder)
   - Имя (firstName + lastName или email)
   - Email
   - Роль (опционально)

2. **Комментарий:**
   - Содержимое (с возможностью обрезки длинных)
   - Дата создания (форматированная)
   - Дата обновления (если отличается от создания)

3. **Пост:**
   - Название (ссылка на пост)
   - Статус публикации (опционально)

4. **Статистика:**
   - Количество лайков
   - Количество дизлайков

---

## 🎨 UI/UX детали

### Состояния компонентов:
- **Loading:** Skeleton loader
- **Error:** UniversalError с кнопкой retry
- **Empty:** UniversalEmpty с текстом поиска (если есть)
- **Success:** Список комментариев

### Интерактивность:
- Hover эффекты на строках
- Disabled состояния во время операций
- Визуальная обратная связь (toast)
- Плавные переходы

### Accessibility:
- Правильные ARIA атрибуты
- Клавиатурная навигация
- Screen reader совместимость
- Focus management

---

## ✅ Чеклист готовности

- [ ] Структура папок создана
- [ ] Типы определены
- [ ] Константы созданы
- [ ] API routes созданы и протестированы
- [ ] API функции созданы
- [ ] Hooks созданы и протестированы
- [ ] UI компоненты созданы
- [ ] Страница создана
- [ ] Навигация обновлена
- [ ] Все функции работают
- [ ] Обработка ошибок реализована
- [ ] Accessibility проверена
- [ ] Производительность оптимизирована

---

**Дата создания:** 2024
**Версия:** 1.0


